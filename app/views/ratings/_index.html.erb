<% sum = 0 %>
<% for rating in @ratings %>
  <% sum = sum + rating.value.to_i%>
<% end %>
<%size = @ratings.size==0 ? 1 : @ratings.size%>
<% @result = sum.to_f / size%>
<%= render :partial => 'ratings/rating'%>
 &nbsp; votes:  <%= @ratings.size %>, avarage: <%= format("%1.1f", @result) %>

